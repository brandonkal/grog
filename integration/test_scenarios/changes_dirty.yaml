name: changes dirty repo
repo: changes_dirty
cases:
  - name: changes_dirty_repo
    setup_command: ../../scripts/setup_changes_repo.sh dirty
    grog_args:
      - changes
      - --since=HEAD
      - --dependents=transitive
